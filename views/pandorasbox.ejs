<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lab 0 | <%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <% if (mode === 1) { %>
    <img src="static/images/<%= message %>" alt="random image" />
    <% } else { %>
    <p><%= message %></p>
    <% } %>
  </body>
</html>
